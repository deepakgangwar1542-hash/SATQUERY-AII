# SatQuery AI — core backend requirements (CPU-only, no heavy ML deps).
# Heavy ML extras live in requirements-ml.txt (torch/transformers/etc.);
# every agent degrades gracefully when they are absent (PRD §7.2, §7.5).
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0
pydantic>=2.6
python-multipart>=0.0.9
numpy>=1.26
pandas>=2.2
rasterio>=1.3.10
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
PyYAML>=6.0
scikit-learn>=1.4
httpx>=0.27
pytest>=8.0
pytest-timeout>=2.3
fpdf2>=2.7
